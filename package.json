{
  "name": "my-porkerstar-data",
  "description": "Firebase functions",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "firebase-login": "firebase login",
    "firebase-init": "firebase init"
  },
  "devDependencies": {
    "firebase-tools": "^11.25.2"
  },
  "dependencies": {
  }
}
